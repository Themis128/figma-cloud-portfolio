# Layer packaging instructions for AWS Lambda

# 1. Create a directory for dependencies
mkdir python

# 2. Install required packages into this directory
pip install transformers torch numpy -t python

# 3. Zip the directory for Lambda Layer upload
zip -r layer.zip python

# 4. Upload layer.zip using AWS Console or boto3
# (see deployment script for boto3 automation)
