# Simple Browser Multi

Modified Simple Browser allowing multiple independent webview browser windows.

Use the command "Simple Browser Multi: Show" to open a new browser window.
Each invocation opens a new independent webview panel.
