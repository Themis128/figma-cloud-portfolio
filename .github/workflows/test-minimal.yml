name: Test Minimal

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-pinned:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4.4.0
      - run: pnpm --version
