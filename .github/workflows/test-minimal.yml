name: Test Minimal

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-no-pnpm-action:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: npm install -g pnpm@latest
      - run: pnpm --version
