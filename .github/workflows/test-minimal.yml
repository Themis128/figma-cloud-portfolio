name: Test Minimal

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-v5:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - run: pnpm --version
